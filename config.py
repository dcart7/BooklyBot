# ============================================================
# config.py — Конфигурация бота
# ============================================================

import os
from dotenv import load_dotenv

load_dotenv()

# ── Токен бота (обязательно заменить или задать через .env) ─
BOT_TOKEN: str = os.getenv("BOT_TOKEN", "YOUR_BOT_TOKEN_HERE")

# ── ID администратора ────────────────────────────────────────
ADMIN_ID: int = int(os.getenv("ADMIN_ID", "123456789"))

# ── Канал для публикации расписания ─────────────────────────
# Пример: "@my_schedule_channel" или числовой ID вида -100123456789
SCHEDULE_CHANNEL_ID: str | int = os.getenv("SCHEDULE_CHANNEL_ID", "@your_schedule_channel")

# ── Канал подписки (обязательная подписка пользователей) ────
CHANNEL_ID: str | int = os.getenv("CHANNEL_ID", "@your_channel")
CHANNEL_LINK: str = os.getenv("CHANNEL_LINK", "https://t.me/your_channel")

# ── Список услуг: название → длительность в минутах ─────────
# Длительность определяет, какое количество слотов будет
# заблокировано при бронировании (блок = 30 мин).
SERVICES: dict[str, int] = {
    "💅 Манікюр":                  90,   # 1.5 год
    "💅 Манікюр + покриття":       120,  # 2 год
    "💅 Манікюр френч":            150,  # 2.5 год
    "🦶 Педикюр":                  120,  # 2 год
    "🦶 Педикюр + покриття":       150,  # 2.5 год
    "🦶 Комбо манікюр + педикюр":  240,  # 4 год
    "🎨 Офарбування брів":          30,  # 0.5 год
    "✨ Ламінування брів":          60,  # 1 год
    "👁️ Нарощування вій":          150,  # 2.5 год
    "👁️ Корекція вій":             90,  # 1.5 год
}

# ── Временной шаг слота (минуты) ────────────────────────────
SLOT_STEP: int = 30          # минимальный блок времени

# ── Рабочие часы по умолчанию ───────────────────────────────
DEFAULT_START_HOUR: int = 9  # 09:00
DEFAULT_END_HOUR: int = 19   # 19:00 (последний слот начинается в 18:30)

# ── На сколько дней вперёд формируется расписание ───────────
SCHEDULE_DAYS_AHEAD: int = 30
